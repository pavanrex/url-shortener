

services:
  web:
    build: .
    container_name: url_shortener_app
    ports:
      - "8000:8000"
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis   #imp

  redis:
    image: redis:7
    container_name: url_shortener_redis
    ports:
      - "6379:6379"